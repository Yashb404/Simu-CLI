[serve]
port = 8080
open = false
# Uncomment to suppress hot-reload noise when needed:
# no_autoreload = true

[[proxy]]
rewrite = "/api/v1/"
backend = "http://127.0.0.1:3001/api/v1/"

[[proxy]]
rewrite = "/auth/"
backend = "http://127.0.0.1:3001/auth/"
